# Copyright (c) Contributors to the RISCV UnifiedDB <https://github.com/riscv/riscv-unified-db>
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../schemas/param_schema.json

$schema: param_schema.json#
kind: parameter
name: PMP_MMODE_ENFORCEMENT
long_name: TODO   # TODO: short human-readable label
description: |
  Optionally, PMP checks may additionally apply to M-mode accesses, in which case the PMP registers themselves are locked, so that even M-mode software cannot change them until the hart is reset.
  # Source: machine.adoc:3349 (classification NORM_DIRECT)
schema:
  type: boolean
definedBy:
  extension:
    name: Sm   # TODO: verify defining extension
