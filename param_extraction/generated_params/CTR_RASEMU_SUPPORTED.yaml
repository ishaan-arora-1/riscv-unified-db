# Copyright (c) Contributors to the RISCV UnifiedDB <https://github.com/riscv/riscv-unified-db>
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../schemas/param_schema.json

$schema: param_schema.json#
kind: parameter
name: CTR_RASEMU_SUPPORTED
long_name: TODO   # TODO: short human-readable label
description: |
  When the optional `__x__ctrctl`.RASEMU bit is implemented and set to 1, transfer recording behavior is altered to emulate the behavior of a return-address stack (RAS).
  # Source: smctr.adoc:669 (classification NORM_DIRECT)
schema:
  type: boolean
definedBy:
  extension:
    name: Smctr   # TODO: verify defining extension
