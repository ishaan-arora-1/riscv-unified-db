# Copyright (c) Contributors to the RISCV UnifiedDB <https://github.com/riscv/riscv-unified-db>
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../schemas/param_schema.json

$schema: param_schema.json#
kind: parameter
name: SSTATUS_UXL_ACCESS
long_name: TODO   # TODO: short human-readable label
description: |
  When SXLEN=64, it is a *WARL* field that encodes the current value of UXLEN. In particular, an implementation may make UXL be a read-only field whose value always ensures that UXLEN=SXLEN.
  # Source: supervisor.adoc:134 (classification NORM_CSR_RW)
schema:
  type: boolean
definedBy:
  extension:
    name: S   # TODO: verify defining extension
