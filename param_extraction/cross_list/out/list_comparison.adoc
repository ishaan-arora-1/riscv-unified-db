//
// SPDX-FileCopyrightText: 2026 Contributors to the RISCV UnifiedDB
// SPDX-License-Identifier: BSD-3-Clause-Clear
//
// GENERATED FILE -- do not edit by hand.
// Regenerate: uv run --python 3.13 --with pyyaml python \
//   param_extraction/cross_list/scripts/generate_asciidoc.py
//
// Pinned sources:
//   James' param_defs : 213450b8671a513ed94cd62fc87a836f8a839a10 (2026-04-03)
//   ISA manual        : 7fc198f13ad89e9608e9404be1c7a8119c14c13b (2026-02-15)
//

= Parameter List Comparison
:toc:
:toclevels: 2

Comparison of our parameter list against James Ball's.

== Standing of the two lists -- read before any count

The two lists do not have equal standing, and no count below should be
read as if they did.

[cols="20,40,40",options="header"]
|===
| | Ours | James'
| Review status | Every entry reviewed individually by Allen Baum and Umer | *Not reviewed by Allen or Umer.* He describes it as an initial individual effort, explicitly not Parameter SIG consensus
| Scope | Only parameters believed *missing* from UDB | The whole parameter space, including parameters UDB already has
| Evidence | A verbatim spec excerpt per entry, all located in the manual | An `impl-def` pointer to a normative rule; no excerpt
| Completeness | 38 entries | 179 entries, most `long-name` and many `type` fields still `TBD`
|===

So an entry appearing on his list is a *candidate*, at the same stage ours were before review. An entry on ours has cleared expert review. When this document says the two lists agree, that means an unreviewed candidate coincides with a reviewed one, which is corroboration for his entry rather than fresh support for ours.

Judgement calls throughout are flagged rather than asserted, per rule 15 of `INCLUSION_CRITERIA.md`: a normative rule whose parameter-ness needs the design rationale cannot be decided mechanically.

== Method, and why not by name

Both lists invented their own parameter names, so name equality is not a
usable join. Measured: only *3* of our 38 names collide with James',
against *19* that share a normative tag. A name-based comparison would
report the two efforts as ~97% disjoint, which is an artefact of naming,
not a real finding.

The primary join is therefore the *normative tag* in the ISA manual:

* ours -- excerpt -> enclosing `[#norm:...]` anchor -> normative rule
* James' -- `impl-def` -> normative rule -> its tag(s)

Coverage of that chain: 37 of our 38 sit in a norm anchor and 35 reach a
rule entry; 135 of James' 179 entries resolve to a tag. Name equality is
reported below as corroboration where it happens, never as the join.

IMPORTANT: The tag join alone is *not* sufficient, and an earlier draft of
this document was wrong because of it. 44 of James' entries carry an
`impl-def` that does not resolve against the pinned manual, and his
`csr_definitions` are hand-authored per register, so wherever he covers a
concept through an unresolved name a tag-only join reports it as ours
alone. Every parameter the tag join left unmatched was therefore checked
by hand against his full file set, by register, field and concept. That
recovered seven further matches, listed with their evidence below.

Relationship vocabulary follows Jordan's `sail_udb_config_mapping.md`:
*Exact* (one-to-one), *Partial* (concepts overlap but not one-to-one --
one umbrella entry against several fine-grained ones, or a register-level
entry against a specific field rule).

== Summary

[cols="60,40",options="header"]
|===
| Measure | Count
| Our parameters (expert-confirmed, de-duplicated) | 38
| James' entries | 179
| On both lists | 26
| ...matched by normative tag | 19
| ...matched by concept, hand-verified | 7
| Arguable -- overlap is a judgement call | 4
| Only on our list | 8
| Only on James' list | 144
|===

== Parameters on both lists

[cols="20,22,16,14,28",options="header"]
|===
| Our name | James' name(s) | Relationship | Matched by | Evidence
| `CTR_CCE_WIDTH` | `CTR_CCOUNTER_IMPL` | Exact | concept (hand-verified) | Same 0..4 exponent-bit choice in CCE. Missed only because his impl-def is `CTR_CCOUNTER_IMPL` while the rule is named `ccounter_impl`.
| `CTR_CYCLE_COUNTING_SUPPORTED` | `CTRDATA_CC_SUPPORTED` | Exact | normative tag | `norm:ctrdata_cc_supported_param`
| `DELEGATABLE_EXCEPTIONS` | `medeleg`, `mideleg` | Partial (1-to-2) | concept (hand-verified) | He records the delegatable-bit mask once per register (`MEDELEG_WARL`, `MIDELEG_WARL`, both unresolved); ours is the single statement covering both.
| `HINT_SXLEN_DEST_REG_BEHAVIOR` | `HINT_SXLEN` | Exact | normative tag | `norm:hint_sxlen_param`
| `HINT_XLEN_REDUCTION_BEHAVIOR` | `XLEN_REDUCTION_HINT_OP` | Exact | normative tag | `norm:xlen_reduction_hint_op1`
| `HIP_BIT_WRITABLE` | `hip` | Partial (register-level) | concept (hand-verified) | His `hip` entry is a register-level WARL umbrella (`HIP_WARL`, unresolved); ours is the per-bit rule conditioned on `sie` bit i being read-only zero.
| `HPM_UNIMPLEMENTED_ACCESS_BEHAVIOR` | `HPM_READ_BEHAVIOR` | Exact | normative tag | `norm:hpm_unimplemented_counter_access`
| `HTVAL_LEGAL_VALUES` | `htval` | Exact | normative tag | `norm:htval_val`
| `MCYCLE_SHARED` | `MCYCLE_SHARED` | Exact | normative tag | `norm:mcycle_shared` +
name also matches
| `MENVCFG_FIOM_READONLY` | `MENVCFG_FIOM_RDONLY0_OK` | Exact | normative tag | `norm:menvcfg_fiom_rdonly0_ok`
| `MIP_WRITABLE_BITS` | `mip` | Exact | normative tag | `norm:mip_bits_wr_or_rdonly`
| `MNEPC_INVALID_ADDRESS_CONVERSION` | `mnepc` | Partial (register-level) | concept (hand-verified) | His `mnepc` entry is register-level WARL (`MNEPC_WARL`, unresolved); ours is specifically the invalid-address conversion.
| `MTVAL2_LEGAL_VALUES` | `mtval2` | Exact | normative tag | `norm:mtval2_val`
| `PMA_IDEMPOTENT_IMPLICIT_READ_SIZE` | `PMA_IDP_IMPLICIT_SZ` | Exact | normative tag | `norm:pma_idp_implicit_sz` +
Domain disagreement: ours is a power-of-2 byte size, his is typed `boolean`.
| `PMPADDR_WARL_MASK` | `pmpaddrN` | Exact | normative tag | `norm:pmp_addr_warl`
| `SCTRDEPTH_SUPPORTED_VALUES` | `SCTRDEPTH_DEPTH` | Exact | normative tag | `norm:sctrdepth-depth_param`
| `SEPC_INVALID_ADDR_BEHAVIOR` | `sepc` | Exact | normative tag | `norm:sepc_acc_invalid_addr`
| `SIP_BITS_ACCESS` | `sip` | Partial (register-level) | concept (hand-verified) | His `sip` entry is register-level WARL (`SIP_WARL`, unresolved); ours is the per-bit writable-or-read-only rule.
| `SSTATUS_UBE_ACCESS` | `sstatus.UBE` | Exact | normative tag | `norm:sstatus-ube`
| `SSTATUS_UXL_ACCESS` | `sstatus.UXL` | Exact | normative tag | `norm:sstatus-uxl_sz`
| `STANDARD_INTERRUPT_SUPPORT` | `SEI_INTR_IMPL`, `STI_INTR_IMPL`, `SSI_INTR_IMPL`, `LCOFI_INTR_IMPL` | Partial (1-to-4) | concept (hand-verified) | He enumerates one parameter per standard interrupt type; ours is the single umbrella sentence naming all four.
| `VSSTATUS_UBE_PARAM` | `VSSTATUS_UBE` | Exact | normative tag | `norm:vsstatus-ube_param`
| `VSXL_RO_PARAM` | `hstatus.VSXL` | Exact | normative tag | `norm:vsxl_ro_param`
| `VTW_VIRTINSTR_PARAM` | `VTW_VIRTINSTR` | Exact | normative tag | `norm:vtw_virtinstr_param`
| `WFI_TW_ALWAYS_ILLEGAL` | `MSTATUS_TW_ALWAYS_ILLEGAL` | Exact | normative tag | `norm:mstatus_tw_always_illegal`
| `XRET_CLEARS_LR_RESERVATION` | `MRET_CLR_LR_RESV`, `SRET_CLR_LR_RESV` | Partial (1-to-2) | concept (hand-verified) | He splits the `__x__RET` rule into MRET and SRET variants.
|===

== Arguable overlaps

Concepts that overlap, where calling it a match is a judgement rather
than a fact. Left unresolved deliberately.

[cols="24,24,52",options="header"]
|===
| Our name | James' nearest | Why it is unresolved
| `CTRTARGET_MISP_IMPLEMENTED` | `ctrtarget` | MISP is verified to be a field of `ctrtarget` (section at smctr.adoc:314, bitfield at :331), so the register is right. But his entry is a register-level WARL umbrella and says nothing about the optional MISP bit being read-only 0 when unimplemented.
| `HPM_MISCONFIGURED_BEHAVIOR` | `HPM_READ_BEHAVIOR` | His `HPM_READ_BEHAVIOR` draws on `HPM_PLATFORM_SPECIFIC_IMPL` and `HPM_UNIMPLEMENTED_COUNTER_ACCESS`. Ours is misconfigured *event selection*, which is a different axis from counter read access; whether his entry is meant to span it is unclear.
| `MSTATEEN_BIT63_TYPE` | `mstateen0/mstateen1/mstateen2/mstateen3` | He has one `ConstMask` entry covering all four `mstateen` registers, which arguably subsumes bit 63; it does not carry the condition (hypervisor absent and matching `sstateen` all read-only zero) that makes ours specific.
| `ZALASR_MISALIGNED_ATOMICITY_GRANULE` | `PMA_MAG_OP_LDST`, `PMA_MAG_EXC` | He has misaligned-atomicity-granule PMA parameters in `machine.yaml`. Ours is the Zalasr-specific relaxation referring to that same PMA. Already flagged separately as overlapping UDB's `MISALIGNED_MAX_ATOMICITY_GRANULE_SIZE`.
|===

== Only on our list

Each of these was verified individually against James' complete file
set: by normative tag, by register name, by field name and by keyword
sweep across all fifteen of his YAML files. None is covered by any
entry of his.

The *Nearest entry he has* column matters when merging: most of these
are not blank areas of his list but a missing sibling or a missing
axis next to something he does have.

[cols="20,22,18,40",options="header"]
|===
| Name | Domain | Tag status | Nearest entry he has
| `CSR_STRONGLY_ORDERED` | subset of the implemented CSRs (possibly empty) | rule not marked `kind: parameter` | Nothing adjacent in his files.
| `CTR_CTRDATA_TYPE_IMPLEMENTED` | {read-write, read-only-0} | rule not marked `kind: parameter` | He has `ctrsource` and `ctrtarget` as WARL registers but no `ctrdata` register entry at all; his only `ctrdata` reference is the cycle-count field. Gap in his own CSR coverage.
| `HGATP_PPN_LOWER_BITS_RO` | {read-write, read-only-0} | rule not marked `kind: parameter` | Nothing adjacent in his files.
| `MSECCFG_SEED_BITS_RW` | {read-write, read-only-0} | no anchor | He has `MSECCFG_USEED_SSEED_RST`, which is the *reset value* of the seed bits. Ours is whether they are writable or a read-only constant zero -- a different axis. Sail treats these as two settings too (`Zkr.sseed_reset_value` vs `Zkr.sseed_read_only_zero`).
| `SENVCFG_FIOM_ACCESS` | {read-write, read-only-0} | rule not marked `kind: parameter` | He has the M-mode sibling `MENVCFG_FIOM_RDONLY0_OK` but not the S-mode one; his only `senvcfg` entry is CBIE. Textbook per-CSR replication gap. Note Jordan's mapping shows UDB has no FIOM writability parameter either, while Sail does (`base.writable_fiom`), so this is a gap in two of the three lists.
| `SISELECT_WIDTH` | supported range 0..N, N >= 0xFFF | rule not marked `kind: parameter` | He has the `miselect` sibling but not `siselect`.
| `STATEEN_IMPLICIT_UPDATE_EXCEPTION` | {true, false} | rule not marked `kind: parameter` | Nothing adjacent in his files.
| `VSISELECT_WIDTH` | supported range 0..N, N >= 0xFFF | rule not marked `kind: parameter` | He has the `miselect` sibling but not `vsiselect`.
|===

== Only on James' list

144 entries. Reporting that as a single number would be
misleading, because it mixes parameters UDB already has (our list
excludes those by design), things outside the ISA parameter space, and
genuinely new candidates. Each entry below was adjudicated by hand.

CAUTION: These verdicts are a *proposed* segmentation, not a reviewed
one. Names could not be used as the key here either -- his names and
UDB's differ -- so the evidence was UDB's `csr_references`, which carry
the CSR and field each parameter controls, plus exact and normalised
name matching, with a hand check on every row. The reason for each
verdict is given so it can be challenged individually.

NOTE: UDB baseline is the union of our 223-parameter
snapshot and the 228 names in Jordan's
`sail_udb_config_mapping.md`, giving *229* distinct UDB
parameters -- which matches the count James reports. The two snapshots
disagree slightly: `MCOUNTINHIBIT_IMPLEMENTED`, `NUM_USABLE_PMP_ENTRIES`, `PMP_NA4_SUPPORTED`, `PMP_NAPOT_SUPPORTED`, `PMP_TOR_SUPPORTED`, `ZAWRS_NTO_IS_NOP` appear only in the newer one, and `MSTATUS_TVM_IMPLEMENTED` only in ours. Verdicts are checked against the union.

[cols="60,40",options="header"]
|===
| Segment | Count
| Already in UDB | 56
| Overlaps UDB, not one-to-one | 30
| No UDB counterpart found | 52
| Outside the ISA parameter space | 5
| Duplicated within his own list | 1
|===

=== Already in UDB (56)

UDB already defines this parameter, sometimes under a different name. Our list excludes anything already in UDB by design, so these are a scope difference and not a gap in our work.

[cols="22,26,52",options="header"]
|===
| James' entry | UDB parameter(s) | Basis
| `LRSC_ALIGNMENT` | `LRSC_MISALIGNED_BEHAVIOR` | His enum {AlwaysMisalignedExc, AlwaysAccessFaultExc, Other} is the same choice; Jordan maps Sail memory.misaligned.exceptions.lrsc to this UDB param.
| `LRSC_RESERVATION_STRATEGY` | `LRSC_RESERVATION_STRATEGY` | Exact name match.
| `CACHE_BLOCK_SIZE` | `CACHE_BLOCK_SIZE` | Exact name match.
| `TIME_CSR_HW_SUPPORT` | `TIME_CSR_IMPLEMENTED` | Same question: does a real hardware time CSR exist.
| `GEILEN` | `NUM_EXTERNAL_GUEST_INTERRUPTS` | Same quantity: number of supported guest external interrupts.
| `HTVAL_GUEST_PAGE_FAULT_TRAPVAL` | `REPORT_GPA_IN_HTVAL_ON_GUEST_PAGE_FAULT` | Same htval-on-guest-page-fault reporting choice.
| `VMIDLEN` | `VMID_WIDTH` | Same quantity, hgatp.VMID width.
| `VSXLEN` | `VSXLEN` | Exact name match.
| `hcounteren` | `HCOUNTENABLE_EN` | csr_references match on hcounteren.
| `hgainp.MODE` | `GSTAGE_MODE_BARE`, `SV32X4_TRANSLATION`, `SV39X4_TRANSLATION`, `SV48X4_TRANSLATION`, `SV57X4_TRANSLATION` | hgatp.MODE (his reg-name is a typo for hgatp). UDB splits this MODE field into GSTAGE_MODE_BARE plus one boolean per SvNNx4 translation mode.
| `hstatus.VSBE` | `VS_MODE_ENDIANNESS` | csr_references match on hstatus.VSBE.
| `vsstatus.UXL` | `VUXLEN` | csr_references match on vsstatus.UXL.
| `vstvec.MODE` | `VSTVEC_MODE_DIRECT`, `VSTVEC_MODE_VECTORED` | UDB splits the MODE field into one boolean per legal mode.
| `MARCHID` | `ARCH_ID_VALUE`, `MARCHID_IMPLEMENTED` | Same marchid value/presence.
| `MCAUSE_EXCCODE_MISALIGNED_VS_FAULT_PRI` | `MISALIGNED_LDST_EXCEPTION_PRIORITY` | Same misaligned-vs-fault exception priority choice.
| `MCONFIGPTR_VAL` | `CONFIG_PTR_ADDRESS` | Same mconfigptr value.
| `MIPMPID` | `IMP_ID_VALUE`, `MIMPID_IMPLEMENTED` | Same mimpid value/presence.
| `MTVEC_RDONLY` | `MTVEC_ACCESS` | UDB's MTVEC_ACCESS enumerates read-only vs writable.
| `MVENDOR_ID_BANK` | `VENDOR_ID_BANK` | Same JEDEC bank field.
| `MVENDOR_ID_OFFSET` | `VENDOR_ID_OFFSET` | Same JEDEC offset field.
| `MXLEN` | `MXLEN` | Exact name match.
| `PMP_ENTRY_COUNT` | `NUM_PMP_ENTRIES` | Same quantity, renamed.
| `PMP_GRANULARITY` | `PMP_GRANULARITY` | Exact name match. Note Jordan flags a units transform vs Sail (G vs G+2).
| `SXLEN` | `SXLEN` | Exact name match.
| `UXLEN` | `UXLEN` | Exact name match.
| `mcounteren` | `MCOUNTENABLE_EN` | csr_references match on mcounteren.
| `mcountinhibit` | `COUNTINHIBIT_EN`, `MCOUNTINHIBIT_IMPLEMENTED` | csr_references match on mcountinhibit.
| `misa.EXTENSIONS` | `MUTABLE_MISA_A`, `MUTABLE_MISA_B`, `MUTABLE_MISA_C`, `MUTABLE_MISA_D`, `MUTABLE_MISA_F`, `MUTABLE_MISA_H`, ... (11 total) | UDB splits misa writability into one boolean per extension bit.
| `misa.MXL` | `MXLEN` | csr_references match on misa.MXL.
| `mstatus.FS` | `MSTATUS_FS_LEGAL_VALUES` | csr_references match on mstatus.FS.
| `mstatus.MBE` | `M_MODE_ENDIANNESS` | csr_references match on mstatus.MBE.
| `mstatus.SBE` | `S_MODE_ENDIANNESS` | csr_references match on mstatus.SBE.
| `mstatus.SXL` | `SXLEN` | csr_references match on mstatus.SXL.
| `mstatus.TVM` | `MSTATUS_TVM_IMPLEMENTED` | csr_references match on mstatus.TVM.
| `mstatus.UBE` | `U_MODE_ENDIANNESS` | csr_references match on mstatus.UBE.
| `mstatus.UXL` | `UXLEN` | csr_references match on mstatus.UXL.
| `mstatus.VS` | `MSTATUS_VS_LEGAL_VALUES` | csr_references match on mstatus.VS.
| `mtval` | `MTVAL_WIDTH` | Implemented-bit mask of mtval is UDB's MTVAL_WIDTH.
| `mtvec.MODE` | `MTVEC_MODES` | csr_references match on mtvec.MODE.
| `MISALIGNED_LDST_HW_SUPPORT` | `MISALIGNED_LDST` | Same hardware-support boolean.
| `hstateen0/hstateen1/hstateen2/hstateen3` | `HSTATEEN_AIA_TYPE`, `HSTATEEN_CONTEXT_TYPE`, `HSTATEEN_CSRIND_TYPE`, `HSTATEEN_ENVCFG_TYPE`, `HSTATEEN_IMSIC_TYPE`, `HSTATEEN_JVT_TYPE` | UDB models one parameter per named hstateen bit.
| `ASIDLEN` | `ASID_WIDTH` | Same quantity, renamed.
| `SATP_MODE_ROZ_SFENCE_ILLEGAL` | `TRAP_ON_SFENCE_VMA_WHEN_SATP_MODE_IS_READ_ONLY` | Same concept; Jordan maps the Sail equivalent to this UDB param.
| `scounteren` | `SCOUNTENABLE_EN` | csr_references match on scounteren.
| `stval` | `STVAL_WIDTH` | Implemented-bit mask of stval is UDB's STVAL_WIDTH.
| `stvec.MODE` | `STVEC_MODE_DIRECT`, `STVEC_MODE_VECTORED` | UDB splits MODE into one boolean per legal mode.
| `ELEN` | `ELEN` | Exact name match.
| `MSTATUS_VS_HW_DIRTY_UPDATE` | `HW_MSTATUS_VS_DIRTY_UPDATE` | Same parameter, word order differs.
| `VECTOR_LS_MISSALIGNED_EXCEPTION` | `VECTOR_LS_MISALIGNED_LEGAL` | Same choice (note his spelling MISSALIGNED).
| `VECTOR_LS_OVERWRITE_PAST_TRAP` | `VECTOR_LOAD_PAST_TRAP` | Same overwrite-past-trap choice.
| `VECTOR_LS_SEG_FF_OVERLOAD` | `VECTOR_LOAD_SEG_FF_OVERWRITE_ELEMENTS_AFTER_FAULT` | Same segment fault-only-first overwrite choice.
| `VECTOR_LS_SEG_PARTIAL_ACCESS` | `VECTOR_LS_SEG_PARTIAL_ACCESS` | Exact name match.
| `VECTOR_LS_WHOLEREG_MISSALIGNED_EXCEPTION` | `VECTOR_LS_WHOLEREG_MISALIGNED_LEGAL` | Same choice (note his spelling MISSALIGNED).
| `VLEN` | `VLEN` | Exact name match.
| `VSSTATUS_VS_EXISTS` | `VSSTATUS_VS_EXISTS` | Exact name match.
| `jvt.BASE` | `JVT_BASE_MASK`, `JVT_BASE_TYPE` | csr_references match on jvt.BASE.
|===

=== Overlaps UDB, not one-to-one (30)

The concept exists in UDB but the granularity or the aspect differs -- typically one register-level entry of his against several field-level UDB parameters, or vice versa.

[cols="22,26,52",options="header"]
|===
| James' entry | UDB parameter(s) | Basis
| `HSXLEN` | `SXLEN` | HS-mode XLEN. UDB models SXLEN, and separately VSXLEN/VUXLEN, without a distinct HSXLEN.
| `MTVAL2_TRAPVAL` | `REPORT_GPA_IN_TVAL_ON_INSTRUCTION_GUEST_PAGE_FAULT`, `REPORT_GPA_IN_TVAL_ON_LOAD_GUEST_PAGE_FAULT`, `REPORT_GPA_IN_TVAL_ON_STORE_AMO_GUEST_PAGE_FAULT` | UDB splits by faulting operation; his is one entry.
| `henvcfg.CBIE` | `FORCE_UPGRADE_CBO_INVAL_TO_FLUSH` | UDB's param controls CBIE=0b11 only; Jordan notes it is related but not the same as the reserved 0b10 behaviour.
| `hstatus.VGEIN` | `NUM_EXTERNAL_GUEST_INTERRUPTS` | VGEIN's legal range is bounded by GEILEN, which UDB models as a count rather than a field constraint.
| `htinst` | `TINST_VALUE_ON_BREAKPOINT`, `TINST_VALUE_ON_LOAD_PAGE_FAULT`, `TINST_VALUE_ON_VIRTUAL_INSTRUCTION` | UDB has 14 TINST_VALUE_ON_* parameters, one per exception; his is one register-level WARL entry. 1-to-many.
| `mtinst` | `TINST_VALUE_ON_BREAKPOINT`, `TINST_VALUE_ON_LOAD_PAGE_FAULT`, `TINST_VALUE_ON_VIRTUAL_INSTRUCTION` | Same 1-to-many relationship as htinst.
| `vsatp.MODE` | `VSSTAGE_MODE_BARE`, `SV39_VSMODE_TRANSLATION`, `SV48_VSMODE_TRANSLATION`, `IGNORE_INVALID_VSATP_MODE_WRITES_WHEN_V_EQ_ZERO` | UDB splits vsatp.MODE across per-mode booleans plus an illegal-write behaviour.
| `vstvec.BASE` | `VSTVEC_MODE_DIRECT`, `VSTVEC_MODE_VECTORED` | UDB has no vstvec BASE-alignment parameter; Jordan records the same gap for stvec.
| `MSTATUS_FS_IMPRECISE` | `HW_MSTATUS_FS_DIRTY_UPDATE` | Adjacent aspect of the same FS dirty-tracking behaviour.
| `MSTATUS_FS_NO_CHANGE_DIRTY` | `HW_MSTATUS_FS_DIRTY_UPDATE` | Adjacent aspect of the same FS dirty-tracking behaviour.
| `MSTATUS_VS_NO_CHANGE_DIRTY` | `HW_MSTATUS_VS_DIRTY_UPDATE` | Adjacent aspect of the same VS dirty-tracking behaviour.
| `MSTATUS_VS_SW_DIRTY_UPDATE` | `HW_MSTATUS_VS_DIRTY_UPDATE` | Adjacent aspect of the same VS dirty-tracking behaviour.
| `MTVAL_PER_EXC_BEHAVIOR` | `REPORT_VA_IN_MTVAL_ON_BREAKPOINT`, `REPORT_ENCODING_IN_MTVAL_ON_ILLEGAL_INSTRUCTION`, `REPORT_VA_IN_MTVAL_ON_LOAD_PAGE_FAULT` | UDB has 14 REPORT_*_IN_MTVAL_ON_* parameters; his is one per-exception umbrella. 1-to-many.
| `menvcfg.CBIE` | `FORCE_UPGRADE_CBO_INVAL_TO_FLUSH` | Same partial relationship as henvcfg.CBIE.
| `menvcfg.PMM` | `PMLEN` | UDB models PMLEN as a configured integer rather than per-CSR PMM field legal values.
| `mhpmcounter` | `HPM_COUNTER_EN` | UDB models which counters exist as a bit vector; his is the register-level WARL.
| `mhpmevent` | `HPM_EVENTS` | UDB enumerates legal event numbers; his is the register-level WARL.
| `mseccfg.PMM` | `PMLEN` | Same partial relationship as menvcfg.PMM.
| `mtvec.BASE` | `MTVEC_BASE_ALIGNMENT_DIRECT`, `MTVEC_BASE_ALIGNMENT_VECTORED` | UDB splits BASE alignment by mode.
| `pmpcfgN.pmpMcfg` | `PMP_TOR_SUPPORTED`, `PMP_NA4_SUPPORTED`, `PMP_NAPOT_SUPPORTED` | UDB splits pmpcfg.A legal values into per-mode booleans.
| `MISALIGNED_LDST_EXC_TYPE` | `MISALIGNED_LDST_EXCEPTION_PRIORITY` | Related but not identical: his is which exception type, UDB's is priority.
| `sstateen0/sstateen1/sstateen2/sstateen3` | `SSTATEEN_JVT_TYPE` | UDB models only the JVT bit at S level, while mstateen/hstateen have six each.
| `senvcfg.CBIE` | `FORCE_UPGRADE_CBO_INVAL_TO_FLUSH` | Same partial relationship as henvcfg.CBIE.
| `VECTOR_FF_PAST_TRAP` | `VECTOR_FF_UPDATE_PAST_TRIM` | Adjacent fault-only-first aspect.
| `VECTOR_FF_SEG_SUBSET_EXC_ON_ZEROTH` | `VECTOR_FF_SEG_EXCEPTION_PARTIAL_LOAD` | He splits the zeroth-element case out; UDB has one parameter.
| `VECTOR_FF_SEG_SUBSET_EXC_PAST_ZEROTH` | `VECTOR_FF_SEG_EXCEPTION_PARTIAL_LOAD` | He splits the past-zeroth case out; UDB has one parameter.
| `VECTOR_LS_VSTART` | `LEGAL_VSTART` | UDB enumerates legal vstart values; his is the load/store-specific rule.
| `VSETVL_WITH_NEW_RATIO_SETS_VILL` | `VILL_SET_ON_RESERVED_VTYPE` | Overlapping vill-setting behaviour.
| `VSETVL_WITH_VILL_1_STAY_1` | `RESERVED_VSET_X0X0_VILL_SET`, `VILL_SET_ON_RESERVED_VTYPE` | Overlapping vill-setting behaviour.
| `jvt.MODE` | `JVT_READ_ONLY` | UDB captures jvt writability but not the MODE legal-value set.
|===

=== No UDB counterpart found (52)

The substance of his contribution. These are candidate new parameters on the same footing as ours, except that they have not been through expert review.

[cols="22,20,58",options="header"]
|===
| James' entry | Domain | Basis
| `hedeleg` | TBD | Delegatable-bit mask of hedeleg. Verified: UDB has no parameter touching any interrupt-pending, interrupt-enable or delegation register.
| `hgeie` | TBD | Writable-bit mask of hgeie. Verified: UDB has no parameter touching any interrupt-pending, interrupt-enable or delegation register.
| `hideleg` | TBD | Delegatable-bit mask of hideleg. Verified: UDB has no parameter touching any interrupt-pending, interrupt-enable or delegation register.
| `hie` | TBD | Writable-bit mask of hie. Verified: UDB has no parameter touching any interrupt-pending, interrupt-enable or delegation register.
| `hvip` | TBD | Writable-bit mask of hvip. Verified: UDB has no parameter touching any interrupt-pending, interrupt-enable or delegation register.
| `vscause` | TBD | WLRL legal exception codes of vscause. UDB has no cause-code legal-value parameter.
| `vsepc` | TBD | WARL behaviour of vsepc. UDB has no vsepc parameter; our SEPC_INVALID_ADDR_BEHAVIOR is the sepc sibling.
| `vsie` | TBD | Writable-bit mask of vsie. Verified: UDB has no parameter touching any interrupt-pending, interrupt-enable or delegation register.
| `vsip` | TBD | Writable-bit mask of vsip. Verified: UDB has no parameter touching any interrupt-pending, interrupt-enable or delegation register.
| `vstval` | TBD | Implemented-bit width of vstval. UDB has MTVAL_WIDTH and STVAL_WIDTH but no VSTVAL_WIDTH.
| `miselect` | TBD | Supported value range of miselect. UDB has no iselect parameter; our SISELECT_WIDTH and VSISELECT_WIDTH are the s/vs siblings.
| `CRITICAL_ERROR` | {ResetHart, ResetPlatform, Other} | Critical-error signalling behaviour. UDB has no equivalent.
| `MCAUSE_RST_ALIAS_OK` | {true, false} | Whether the mcause reset value may alias a defined cause. UDB models no CSR reset values.
| `MCAUSE_RST_VAL` | TBD | Reset value of mcause. UDB models no CSR reset values.
| `MEPC_INV_ADDR_CONV` | {NoInvAddrConv, Other} | Invalid-address conversion on writes to mepc. UDB has no such parameter; our MNEPC_INVALID_ADDRESS_CONVERSION is the RNMI sibling.
| `MHARTID` | TBD | mhartid value. Jordan records platform.hartid as having no UDB parameter. Arguably platform rather than ISA; flagged rather than assumed.
| `MSECCFG_MML_MMWP_RLB_RST` | {true, false} | Reset value of mseccfg MML/MMWP/RLB. UDB models no CSR reset values.
| `MSECCFG_USEED_SSEED_RST` | {true, false} | Reset value of the seed bits. Sail has Zkr.sseed_reset_value/useed_reset_value; Jordan records UDB as having neither.
| `MSTATUS_SBE_ROCOPY` | {true, false} | mstatus.SBE as a read-only copy of MBE. This is the read-only-mirrors-another-bit WARL shape our own list treats as a parameter (cf. SSTATUS_UBE_ACCESS); UDB models only the endianness value.
| `MSTATUS_UBE_ROCOPY` | {ROCopyOfMBE, ROCopyOfSBE, Other} | mstatus.UBE as a read-only copy of MBE. Same shape as MSTATUS_SBE_ROCOPY.
| `MTVAL_INV_ADDR_CONV` | {NoInvAddrConv, Other} | Invalid-address conversion on writes to mtval. UDB has no such parameter.
| `NMI_MCAUSE_VAL` | {true, false} | mcause value on NMI. UDB models no NMI cause value.
| `PMA_CACHE_MM_ALL_ATOMICS` | {true, false} | Whether cacheable main memory supports all atomics. Same PMA gap.
| `PMA_MM_IFETCH` | {true, false} | Whether main-memory regions support instruction fetch. UDB has PMA_GRANULARITY only; Jordan records UDB as having no per-region PMA attributes.
| `PMA_MO_DYN` | {true, false} | Whether memory-ordering PMAs are dynamically configurable. Same PMA gap.
| `PMP_A_L_RST` | {true, false} | Reset value of pmpcfg A and L fields. UDB models no CSR reset values.
| `WFI_OPT_U_MODE` | {true, false} | Whether WFI is available in U-mode. Jordan records platform.wfi_available_to_user_mode as having no UDB parameter. NOTE: our own WFI_U_MODE was rejected by the mentor as a duplicate, so this needs his ruling.
| `WFI_RESUME_REASON` | {true, false} | What may cause WFI to resume. UDB has no WFI resume parameter.
| `mcause.EXCCODE` | TBD (legal enum) | WLRL legal exception codes of mcause. UDB has no cause-code legal-value parameter.
| `mepc` | per bit: constant mask | WARL behaviour of mepc. UDB has no mepc parameter; our SEPC_INVALID_ADDR_BEHAVIOR is the sepc sibling.
| `mie` | per bit: constant mask | Writable-bit mask of mie. Verified: UDB has no parameter touching any interrupt-pending, interrupt-enable or delegation register.
| `mseccfg.MML` | per bit: constant mask | WARL behaviour of mseccfg.MML. UDB has no mseccfg field parameters.
| `mseccfg.MMWP` | per bit: constant mask | WARL behaviour of mseccfg.MMWP. UDB has no mseccfg field parameters.
| `mseccfg.RLB` | per bit: constant mask | WARL behaviour of mseccfg.RLB. UDB has no mseccfg field parameters.
| `mstatus.MDT` | per bit: constant mask | WARL behaviour of mstatus.MDT (Smdbltrp). UDB has no MDT parameter.
| `mstatus.MPP` | TBD (legal enum) | Legal values of mstatus.MPP. UDB models no MPP legal-value set.
| `mstatus.SPP` | TBD (legal enum) | Legal values of mstatus.SPP. UDB models no SPP legal-value set.
| `mstatus.TSR` | per bit: constant mask | WARL behaviour of mstatus.TSR. UDB has MSTATUS_TVM_IMPLEMENTED but no TSR equivalent.
| `mstatus.TW` | per bit: constant mask | WARL behaviour of mstatus.TW. UDB has MSTATUS_TVM_IMPLEMENTED but no TW equivalent.
| `mncause` | TBD | WARL behaviour of mncause (RNMI). UDB has no mncause parameter.
| `mnstatus` | TBD | WARL behaviour of mnstatus (RNMI). UDB has no mnstatus parameter.
| `ctrsource` | TBD | WARL behaviour of ctrsource. UDB has MCTRCTL_* field parameters but nothing for the entry registers.
| `sctrstatus.WRPTR` | TBD | WARL behaviour of sctrstatus.WRPTR. UDB has no sctrstatus parameter.
| `scause.EXCEPTION_CODE` | TBD | WLRL legal exception codes of scause. UDB has no cause-code legal-value parameter.
| `sie` | TBD | Writable-bit mask of sie. Verified: UDB has no parameter touching any interrupt-pending, interrupt-enable or delegation register.
| `sstatus.SDT` | TBD | WARL behaviour of sstatus.SDT (Ssdbltrp). UDB has no SDT parameter.
| `stvec.BASE` | TBD | BASE alignment of stvec. Jordan records explicitly that UDB has no STVEC base-alignment parameter while Sail does.
| `MSTATUS_VS_EXISTS` | {true, false} | Whether mstatus.VS exists when misa.V is clear. UDB has the VS-mode sibling VSSTATUS_VS_EXISTS but not the M-mode one.
| `VECTOR_LS_CONSTANT_STRIDE_X0` | {Always, Never, Other} | Constant-stride vector load/store with rs2=x0. UDB has no equivalent.
| `INTERRUPTS_ALLOWED_IN_PUSHPOP` | {true, false} | Whether interrupts may be taken during Zcmp push/pop sequences. UDB has no equivalent. Our pipeline found this and held it back under rule 1, because the sentence sits in a NOTE block. He includes it.
| `NTL_TRAP_BEHAVIOR` | {true, false} | Trap behaviour for NTL hints. UDB has TRAP_ON_RESERVED_INSTRUCTION but nothing NTL-specific.
| `ZILSD_ALIGN8_ATOMIC` | {true, false} | Whether 8-byte-aligned Zilsd accesses are atomic. UDB has no equivalent.
|===

=== Outside the ISA parameter space (5)

Platform integration or quantities with no architectural units. These fail our observable-and-testable test, so we would exclude them; that is a definition difference, not a defect in his list.

[cols="22,20,58",options="header"]
|===
| James' entry | Domain | Basis
| `MSIP_UPDATE_MAX_TIME` | TBD | A maximum time with no architectural units. Excluded by rule 6 (implementation-defined with no testable units or enumerable options).
| `MSTATUS_TW_TIME_LIMIT` | TBD | The WFI bounded time limit. Excluded by INCLUSION_CRITERIA rule 6, which names "bounded time limit" as its literal example of an implementation-defined quantity with no testable units.
| `MTIME_TICK_PERIOD` | TBD | Platform timing. Excluded by rule 4 (NON_ISA, outside ISA scope); Jordan records platform.clock_frequency and instructions_per_tick as having no UDB parameter either.
| `NMI_ADDR` | TBD | NMI vector address. Excluded by rule 4 (NON_ISA, platform rather than ISA), not on testability grounds -- it is observable. Scope question for the mentor.
| `RST_ADDR` | TBD | Reset vector address. Excluded by rule 4 (NON_ISA, platform rather than ISA), not on testability grounds -- it is observable. Scope question for the mentor.
|===

=== Duplicated within his own list (1)

Recorded twice in his files.

[cols="22,20,58",options="header"]
|===
| James' entry | Domain | Basis
| `sctrdepth.DEPTH` | TBD | Duplicates his own SCTRDEPTH_DEPTH parameter_definition (same field, recorded once in each section). That parameter already matches our SCTRDEPTH_SUPPORTED_VALUES.
|===

=== What the new candidates cluster into

Grouping them shows they are not scattered. Four families account for
most of the list, and each is a category neither UDB nor our own
pipeline covers:

* *Interrupt and delegation bit masks* -- `hedeleg`, `hideleg`, `hie`,
  `hvip`, `hgeie`, `vsie`, `vsip`, `mie`, `sie`. Verified against UDB
  by name and by `csr_references`: UDB has *no* parameter touching any
  interrupt-pending, interrupt-enable or delegation register -- not
  `mip`, not `sip`, none of them. The only near neighbour is
  `NUM_EXTERNAL_GUEST_INTERRUPTS`, which is a count. Our list adds five
  here (`MIP_WRITABLE_BITS`, `SIP_BITS_ACCESS`, `HIP_BIT_WRITABLE`,
  `DELEGATABLE_EXCEPTIONS`, `STANDARD_INTERRUPT_SUPPORT`) and his adds
  nine, so between them the two candidate lists fill a family UDB is
  missing entirely. This is the strongest joint result in the
  comparison.
* *Trap CSR WARL behaviour* -- `mepc`, `vsepc`, `mcause`, `scause`,
  `vscause`, `mncause`, `mnstatus`, `vstval`. Our list has the `sepc`
  sibling; UDB has widths for `mtval`/`stval` only.
* *CSR reset values* -- `MCAUSE_RST_VAL`, `PMP_A_L_RST`,
  `MSECCFG_*_RST`, `NMI_MCAUSE_VAL`. UDB models no reset values at
  all. This is the clearest whole category he adds.
* *PMA attributes* -- `PMA_MM_IFETCH`, `PMA_CACHE_MM_ALL_ATOMICS`,
  `PMA_MO_DYN`. Jordan records the same gap from the Sail side.

Two entries need a mentor ruling rather than a verdict:

* `WFI_OPT_U_MODE` -- Whether WFI is available in U-mode. Jordan records platform.wfi_available_to_user_mode as having no UDB parameter. NOTE: our own WFI_U_MODE was rejected by the mentor as a duplicate, so this needs his ruling.
* `INTERRUPTS_ALLOWED_IN_PUSHPOP` -- our pipeline found this and held
  it back under rule 1, because the sentence sits in a `NOTE` block.
  He includes it. Whichever way it goes, the two lists should agree.

== Applying our inclusion criteria to his list

Everything above compares the two lists *as they stand*. But they were
not built to the same standard. Our list was filtered through
`param_extraction/INCLUSION_CRITERIA.md` at every stage; his was never
held to that ruleset, because he was not working to it. Comparing raw
counts therefore compares a filtered list against an unfiltered one.

This section applies the mechanically checkable rules to his entries so
both lists can be judged on the same footing.

IMPORTANT: A rule hit is *not* a defect in his work. It means the entry
would need review under our criteria, which until the mentor rules on it
is a definition difference and nothing more. Several of these are cases
where our rule may be the thing that is wrong.

=== How the check was done

His entries carry no excerpt, only an `impl-def` pointing at a normative
rule, so the spec text had to be recovered first:

  impl-def -> normative rule -> tag -> anchor text in the .adoc

Rules 4, 10, 11, 12, 13 and 15 need judgement or UDB context and are not
decided mechanically; rule 11 (duplicates) is already covered by the UDB
segmentation above.

Two allowances are built in, both from our own ruleset, without which
the check reports far more hits than are real:

* a *WARL* or *WLRL* field satisfies the inclusion signal on its own,
  per the mentor's standing rule that WARL is automatically a parameter;
* a declared value, width, size or ID counts even with no modal word,
  per rule 5a. Without this, `MXLEN` itself would be flagged.

=== Stage 1 -- before applying the criteria

[cols="70,30",options="header"]
|===
| Measure | Count
| His entries | 179
| ...of which already in UDB | 56
| ...overlapping UDB, not one-to-one | 30
| ...*candidate new parameters* | 52
|===

=== Stage 2 -- what the criteria change

[cols="70,30",options="header"]
|===
| Measure | Count
| His entries | 179
| Spec text recoverable, so checkable | 135
| *Not* recoverable -- unresolved tag, no check possible | 44
| Flagged by at least one rule | 14
|===

Only 14 of the 135 checkable entries are
flagged. That is the headline result of this section: *his list is
largely consistent with our criteria even though it was not built to
them*. Two independent methods, one tag-driven and one prose-driven,
converge on nearly the same standard.

The flagged entries, each verified by reading the spec text:

[cols="26,14,60",options="header"]
|===
| His entry | Rule | Why
| `CRITICAL_ERROR` +
[.small]#machine.adoc:553# | S1, 1 | no inclusion signal -- no optionality language, no WARL/WLRL and no declared value or width in the tagged text; rule 1 -- the tagged text sits in a NOTE/TIP/WARNING block
| `INTERRUPTS_ALLOWED_IN_PUSHPOP` +
[.small]#zc.adoc:1185# | 1 | rule 1 -- the tagged text sits in a NOTE/TIP/WARNING block
| `LRSC_ALIGNMENT` +
[.small]#a-st-ext.adoc:126# | S1 | no inclusion signal -- no optionality language, no WARL/WLRL and no declared value or width in the tagged text
| `MCAUSE_RST_ALIAS_OK` +
[.small]#machine.adoc:2848# | 1 | rule 1 -- the tagged text sits in a NOTE/TIP/WARNING block
| `MSIP_UPDATE_MAX_TIME` +
[.small]#machine.adoc:1464# | S1 | no inclusion signal -- no optionality language, no WARL/WLRL and no declared value or width in the tagged text
| `MSTATUS_TW_TIME_LIMIT` +
[.small]#machine.adoc:792# | 6 | rule 6 -- no testable units, or 'misconfigured'
| `MTIME_TICK_PERIOD` +
[.small]#machine.adoc:2550# | S1, 2 | no inclusion signal -- no optionality language, no WARL/WLRL and no declared value or width in the tagged text; rule 2 -- a fixed requirement (must/shall/always) with no optionality
| `MTVEC_RDONLY` +
[.small]#machine.adoc:1217# | S1, 2 | no inclusion signal -- no optionality language, no WARL/WLRL and no declared value or width in the tagged text; rule 2 -- a fixed requirement (must/shall/always) with no optionality
| `NTL_TRAP_BEHAVIOR` +
[.small]#zihintntl.adoc:162# | S1 | no inclusion signal -- no optionality language, no WARL/WLRL and no declared value or width in the tagged text
| `PMA_CACHE_MM_ALL_ATOMICS` +
[.small]#machine.adoc:3023# | 1 | rule 1 -- the tagged text sits in a NOTE/TIP/WARNING block
| `PMA_MM_IFETCH` +
[.small]#machine.adoc:2986# | S1, 2 | no inclusion signal -- no optionality language, no WARL/WLRL and no declared value or width in the tagged text; rule 2 -- a fixed requirement (must/shall/always) with no optionality
| `vscause` +
[.small]#hypervisor.adoc:1356# | 14 | rule 14 -- legal values stated as identical to another register's, so possibly derived rather than an independent choice
| `vsepc` +
[.small]#hypervisor.adoc:1338# | 14 | rule 14 -- legal values stated as identical to another register's, so possibly derived rather than an independent choice
| `vstval` +
[.small]#hypervisor.adoc:1374# | 14 | rule 14 -- legal values stated as identical to another register's, so possibly derived rather than an independent choice
|===

=== Stage 3 -- the comparison after applying the criteria

[cols="70,30",options="header"]
|===
| Measure | Count
| His candidate new parameters, before | 52
| ...flagged by our criteria | 9
| *Surviving our criteria* | 43
| ...of those, unverifiable (no reachable spec text) | 12
|===

So his genuinely new, criteria-passing contribution is about
*43 parameters*, against our
38. Both numbers carry caveats pointing in opposite directions: ours
are expert-reviewed but were mined from prose and are known to be
non-exhaustive; his are unreviewed, and 12 of
them cannot be checked against spec text at all until the unresolved
tags are settled.

The nine flagged new candidates, and why each matters:

* `CRITICAL_ERROR` (rule S1, 1) -- Critical-error signalling behaviour. UDB has no equivalent.
* `INTERRUPTS_ALLOWED_IN_PUSHPOP` (rule 1) -- Whether interrupts may be taken during Zcmp push/pop sequences. UDB has no equivalent. Our pipeline found this and held it back under rule 1, because 
* `MCAUSE_RST_ALIAS_OK` (rule 1) -- Whether the mcause reset value may alias a defined cause. UDB models no CSR reset values.
* `NTL_TRAP_BEHAVIOR` (rule S1) -- Trap behaviour for NTL hints. UDB has TRAP_ON_RESERVED_INSTRUCTION but nothing NTL-specific.
* `PMA_CACHE_MM_ALL_ATOMICS` (rule 1) -- Whether cacheable main memory supports all atomics. Same PMA gap.
* `PMA_MM_IFETCH` (rule S1, 2) -- Whether main-memory regions support instruction fetch. UDB has PMA_GRANULARITY only; Jordan records UDB as having no per-region PMA attributes.
* `vscause` (rule 14) -- WLRL legal exception codes of vscause. UDB has no cause-code legal-value parameter.
* `vsepc` (rule 14) -- WARL behaviour of vsepc. UDB has no vsepc parameter; our SEPC_INVALID_ADDR_BEHAVIOR is the sepc sibling.
* `vstval` (rule 14) -- Implemented-bit width of vstval. UDB has MTVAL_WIDTH and STVAL_WIDTH but no VSTVAL_WIDTH.

Two are worth singling out:

* `INTERRUPTS_ALLOWED_IN_PUSHPOP` is tagged as literally
  `NOTE: It is implementation defined whether interrupts can also be
  taken during the sequence execution.` Our pipeline found this and
  held it back under rule 1. The audit independently confirms that
  call -- and it is the clearest case where the two lists disagree
  because of a rule, not because of what either of us saw.
* `vsepc`, `vscause` and `vstval` are each spec'd as holding "the same
  set of values that `sepc` / `scause` / `stval` can hold". Under rule
  14 that is derived rather than an independent choice. Whether a
  register that tracks another still deserves its own parameter is a
  real question for the mentor, not something to settle here.

=== Where our rule may be the problem, not his entry

Three of the flags point at our ruleset rather than at his list:

* Rule 1 excludes NOTE blocks outright. But
  `INTERRUPTS_ALLOWED_IN_PUSHPOP` and `MCAUSE_RST_ALIAS_OK` are both
  tagged `[#norm:...]` *inside* a NOTE. The manual's own authors
  marked that text as normative. A blanket NOTE exclusion may be too
  strong when the text carries a normative tag.
* `MTVEC_RDONLY`, `MTIME_TICK_PERIOD` and `PMA_MM_IFETCH` are flagged
  under rule 2 because the *tagged sentence* is a fixed requirement --
  for example `MTVEC_RDONLY` points at "The `mtvec` register must
  always be implemented", which says nothing about read-only. The
  parameter may well be real; the tag is pointing at the wrong
  sentence. That is a fixable defect in his source pointer, and it is
  worth telling him.
* Rule 6 catches `MSTATUS_TW_TIME_LIMIT`, the WFI bounded time limit.
  Our own `INCLUSION_CRITERIA` names "bounded time limit" as its
  example of an untestable quantity, so on our rules this is correctly
  excluded -- but he independently judged it a parameter, which is a
  second vote for revisiting that rule.

== Findings for the ISA manual

Fallout from the comparison that is directly actionable in the manual
repo. These are independent factual claims about the manual's
metadata; they are deliberately *not* offered as an explanation of why
the two lists differ (see <<why-not-causal>>).

=== 24 rules that are parameters but are not marked as such

Each carries a normative tag and has a rule entry in
`normative_rule_defs/`, but the rule is not marked `kind: parameter`.
Each was individually confirmed as a real parameter by Allen Baum and
Umer, so the marking is missing. One-line fix per rule.

* `norm:csr_strongly_ordered` -- our `CSR_STRONGLY_ORDERED` (zicsr.adoc:244)
* `norm:ctrtarget_misp` -- our `CTRTARGET_MISP_IMPLEMENTED` (smctr.adoc:319)
* `norm:ccounter_impl` -- our `CTR_CCE_WIDTH` (smctr.adoc:654) (James has this too)
* `norm:ctrdata_sz_acc` -- our `CTR_CTRDATA_TYPE_IMPLEMENTED` (smctr.adoc:339)
* `norm:hgatp-ppn_op` -- our `HGATP_PPN_LOWER_BITS_RO` (hypervisor.adoc:1083)
* `norm:hip_acc` -- our `HIP_BIT_WRITABLE` (hypervisor.adoc:533) (James has this too)
* `norm:hpm_misconfigured_event_behavior` -- our `HPM_MISCONFIGURED_BEHAVIOR` (counters.adoc:214)
* `norm:hpm_unimplemented_counter_access` -- our `HPM_UNIMPLEMENTED_ACCESS_BEHAVIOR` (counters.adoc:212) (James has this too)
* `norm:htval_val` -- our `HTVAL_LEGAL_VALUES` (hypervisor.adoc:957) (James has this too)
* `norm:mnepc_invalid_convert` -- our `MNEPC_INVALID_ADDRESS_CONVERSION` (rnmi.adoc:64) (James has this too)
* `norm:mstateen-bit-63_roz` -- our `MSTATEEN_BIT63_TYPE` (smstateen.adoc:175)
* `norm:mtval2_val` -- our `MTVAL2_LEGAL_VALUES` (hypervisor.adoc:1843) (James has this too)
* `norm:pmp_addr_warl` -- our `PMPADDR_WARL_MASK` (machine.adoc:3401) (James has this too)
* `norm:sctrdepth-depth_param` -- our `SCTRDEPTH_SUPPORTED_VALUES` (smctr.adoc:215) (James has this too)
* `norm:senvcfg-fiom_acc` -- our `SENVCFG_FIOM_ACCESS` (supervisor.adoc:840)
* `norm:sepc_acc_invalid_addr` -- our `SEPC_INVALID_ADDR_BEHAVIOR` (supervisor.adoc:559) (James has this too)
* `norm:sip_acc` -- our `SIP_BITS_ACCESS` (supervisor.adoc:400) (James has this too)
* `norm:siselect_min_range` -- our `SISELECT_WIDTH` (indirect-csr.adoc:148)
* `norm:sstatus-ube` -- our `SSTATUS_UBE_ACCESS` (supervisor.adoc:227) (James has this too)
* `norm:sstatus-uxl_sz` -- our `SSTATUS_UXL_ACCESS` (supervisor.adoc:143) (James has this too)
* `norm:sip_sie_unimpl` -- our `STANDARD_INTERRUPT_SUPPORT` (supervisor.adoc:465) (James has this too)
* `norm:smstateen_implicit_state_update` -- our `STATEEN_IMPLICIT_UPDATE_EXCEPTION` (smstateen.adoc:100)
* `norm:vsiselect_min_range` -- our `VSISELECT_WIDTH` (indirect-csr.adoc:239)
* `norm:zalasr_misaligned_pma_relax` -- our `ZALASR_MISALIGNED_ATOMICITY_GRANULE` (zalasr.adoc:26)

[[why-not-causal]]
==== Why this is not the reason the lists differ

An earlier draft of this document claimed the missing `kind: parameter`
marking explained why James' list lacks these. The data does not
support that: he has 13 of the 24 anyway. His method is
not purely a selection on that marking -- he also hand-authors
`csr_definitions` per register, which routes around it. The metadata
gap is real and worth fixing on its own merits; it is not a causal
account of the difference between the lists.


=== 2 anchors with no normative rule entry

* `norm:medeleg_mideleg_warl` -- our `DELEGATABLE_EXCEPTIONS` (machine.adoc:1313)
* `norm:xret_clr_lr_resv` -- our `XRET_CLEARS_LR_RESERVATION` (machine.adoc:2704)

=== 1 confirmed parameter with no tag at all

* our `MSECCFG_SEED_BITS_RW` (scalar-crypto.adoc:3772) -- normative text carrying no anchor

=== Defects noticed in James' files while verifying

All found while checking his entries one by one, and all worth
passing back to him.

* `hypervisor.yaml:106` declares `reg-name: hgainp`, but its own
  `impl-def` is `HGATP_MODE_WARL`. `hgainp` is not a RISC-V CSR;
  this is a typo for `hgatp`.
* `machine.yaml` names a parameter `MIPMPID` whose `impl-def` is
  `IMP_ID`. The CSR is `mimpid`, so this looks like a typo.
* `v-st-ext.yaml` spells two parameters `..._MISSALIGNED_...`
  (`VECTOR_LS_MISSALIGNED_EXCEPTION`,
  `VECTOR_LS_WHOLEREG_MISSALIGNED_EXCEPTION`) with a doubled S.
* `smctr.yaml` covers `ctrsource` and `ctrtarget` as WARL registers
  but has no entry for `ctrdata`, even though the spec makes every
  field within `ctrdata` optional and read-only 0 when unimplemented.
* `SCTRDEPTH_DEPTH` appears as a `parameter_definitions` entry and
  `sctrdepth.DEPTH` again under `csr_definitions` -- the same field
  recorded twice.

One asymmetry in UDB is worth raising alongside these: UDB defines
`MSTATUS_TVM_IMPLEMENTED` but has nothing for `mstatus.TSR` or
`mstatus.TW`, which are the same shape of choice. James has all three.
If TVM is a parameter then TSR and TW should be too.


=== 44 of James' entries reference an unresolved tag

These `impl-def` names have no matching normative rule in the pinned
manual (7fc198f1, 2026-02-15). His definitions are about seven
weeks newer, so some may already exist upstream and others are likely
tags he is proposing. Worth confirming with him rather than assuming.

This is not only a documentation gap. It is what made a tag-only join
unsafe: seven of the matches above are invisible to the tag join purely
because the counterpart entry resolves through one of these names.

* `ASIDLEN`, `CACHE_BLOCK_SIZE`, `CTR_CCOUNTER_IMPL`, `EE_IMPL_FLEX`, `HEDELEG_WARL`, `HGEIE_WARL`, `HIDELEG_WARL`, `HIE_WARL`, `HIP_WARL`, `HVIP_WARL`, `LCOFI_INTR_IMPL`, `MCAUSE_EXCCODE_MISALIGNED_VS_FAULT_PRI`, `MCONFIGPTR_VAL`, `MEDELEG_WARL`, `MENVCFG_PMM_WARL`, `MIE_WARL`, `MISALIGNED_LDST_EXC_TYPE`, `MISALIGNED_LDST_HW_SUPPORT`, `MNCAUSE_WARL`, `MNSTATUS_WARL`, `MRET_CLR_LR_RESV`, `MSTATUSH_MBE_WARL`, `MSTATUSH_SBE_WARL`, `MSTATUSH_UBE_WARL`, `MSTATUS_MBE_WARL`, `MSTATUS_SBE_WARL`, `MSTATUS_UBE_WARL`, `MSTATUS_UXL_WARL`, `PMPCFG_WARL`, `PMP_GRANULARITY`, `SCTRDEPTH_DEPTH_WARL`, `SEI_INTR_IMPL`, `SIE_WARL`, `SIP_WARL`, `SRET_CLR_LR_RESV`, `SSI_INTR_IMPL`, `STI_INTR_IMPL`, `STVEC_BASE_WARL`, `STVEC_MODE_WARL`, `VSIE_WARL`, `VSIP_WARL`, `VSTVEC_BASE_WARL`, `VSTVEC_MODE_WARL`
